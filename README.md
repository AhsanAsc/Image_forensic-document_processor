# Image Forensics

**Task:** Detect orientation, deskew to 0°, crop document boundaries, and return the corrected image with consistent padding. Outputs the **rotation angle** and the **cropped image** (saved or Base64).  
Language: **Python 3.12+**, dependency manager: **Poetry**. :contentReference[oaicite:4]{index=4}

## Install
```bash
poetry install
